import React from "react";
import Perfil from "@/components/Perfil/Perfil";

export const metadata = {
  title: "Ivess | Perfil",
};

const App = () => {
  return (
    <html>
      <Perfil />
    </html>
  );
};

export default App;
