import React from "react";
import Perfil from "@/components/Perfil/Perfil";

export const metadata = {
  title: "Ivess | Perfil",
};

const App = () => {
  return (
      <Perfil />
  );
};

export default App;
